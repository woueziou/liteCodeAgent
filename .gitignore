node_modules/
bun.lockb
*.log
.DS_Store
# Default tickets.autoStateFile (config.ts); a project overriding that path must add its
# own entry here — this ignore only covers the default.
.claude/data/ticket-sync-auto-state.json
# Generated by `litecode dashboard --build` (default --out path). Never commit this: it's
# a snapshot of the ticket buffer at build time, and a committed copy would silently go
# stale the moment any ticket's status/priority/size changes — regenerate locally instead.
docs/dashboard.html
