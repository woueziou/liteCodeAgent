{
  "name": "lite-code-agent",
  "version": "0.4.0",
  "description": "Portable, versioned multi-agent pipeline packs with Claude Code and direct API runners.",
  "type": "module",
  "bin": { "litecode": "./src/cli.ts" },
  "scripts": {
    "litecode": "bun run src/cli.ts",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": { "bun": ">=1.1.0" }
}
